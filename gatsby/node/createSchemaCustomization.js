const createSchemaCustomization = ({ actions }) => {
  // const { createTypes } = actions
  // const typeDefs = `
  //   type LandingPageJson implements Node @infer(noDefaultResolvers: true) {
  //     id: ID!
  //   }
  // `
  // createTypes(typeDefs)
}

module.exports = createSchemaCustomization
