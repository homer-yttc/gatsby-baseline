pre-commit:
  parallel: false
  commands:
    formatter:
      glob: "*.{js,jsx,scss}"
      run: prettier -c {staged_files}
    lint-check:
      glob: "*.{js,jsx}"
      run: eslint {staged_files} --quiet
    sass-check:
      glob: "*.{scss}"
      run: stylelint --quiet {staged_files}
